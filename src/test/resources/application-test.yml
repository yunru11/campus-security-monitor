spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:monitor;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
ai:
  event:
    token: test-token
    min-confidence: 0.25
monitor:
  auth:
    username: admin
    password: Monitor@2026
